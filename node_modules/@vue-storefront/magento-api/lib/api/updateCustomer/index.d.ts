import { FetchResult } from '@apollo/client/core';
import { CustomQuery } from '@vue-storefront/core';
import { CustomerUpdateInput, UpdateCustomerMutation } from '../../types/GraphQL';
import { Context } from '../../types/context';
/**
 * Updates the data of the current customer. To override the default query, use the `updateCustomer` query key.
 */
declare const _default: (context: Context, input: CustomerUpdateInput, customQuery?: CustomQuery) => Promise<FetchResult<UpdateCustomerMutation>>;
export default _default;
