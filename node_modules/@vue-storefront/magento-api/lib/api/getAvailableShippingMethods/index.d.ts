import { ApolloQueryResult } from '@apollo/client/core';
import { CustomQuery } from '@vue-storefront/core';
import { Context } from '../../types/context';
import { GuestAvailableShippingMethodsQuery } from '../../types/GraphQL';
declare const _default: (context: Context, params: {
    cartId: string;
}, customQuery?: CustomQuery) => Promise<ApolloQueryResult<GuestAvailableShippingMethodsQuery>>;
export default _default;
