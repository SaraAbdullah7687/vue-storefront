import { FetchResult } from '@apollo/client/core';
import { CustomQuery } from '@vue-storefront/core';
import { CreateCustomerMutation, CustomerCreateInput } from '../../types/GraphQL';
import { Context } from '../../types/context';
/**
 * Registers a new customer. To override the default query, use the `createCustomer` query key.
 */
declare const _default: (context: Context, input: CustomerCreateInput, customQuery?: CustomQuery) => Promise<FetchResult<CreateCustomerMutation>>;
export default _default;
