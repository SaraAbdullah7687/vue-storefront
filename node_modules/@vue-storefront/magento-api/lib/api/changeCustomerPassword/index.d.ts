import { FetchResult } from '@apollo/client/core';
import { CustomQuery } from '@vue-storefront/core';
import { ChangeCustomerPasswordMutation } from '../../types/GraphQL';
import { Context } from '../../types/context';
/**
 * Changes password of the current customer. To override the default query, use the `changeCustomerPassword` query key.
 */
declare const _default: (context: Context, params: {
    currentPassword: string;
    newPassword: string;
}, customQuery?: CustomQuery) => Promise<FetchResult<ChangeCustomerPasswordMutation>>;
export default _default;
