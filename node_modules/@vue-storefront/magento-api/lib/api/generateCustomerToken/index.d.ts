import { FetchResult } from '@apollo/client/core';
import { CustomQuery } from '@vue-storefront/core';
import { GenerateCustomerTokenMutation } from '../../types/GraphQL';
import { Context } from '../../types/context';
/**
 * Logs in the customer based on provided username and password. To override the default query, use the `generateCustomerToken` query key.
 */
declare const _default: (context: Context, params: {
    email: string;
    password: string;
    recaptchaToken: string;
}, customQuery?: CustomQuery) => Promise<FetchResult<GenerateCustomerTokenMutation>>;
export default _default;
