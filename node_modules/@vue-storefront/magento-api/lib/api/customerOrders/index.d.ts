import { ApolloQueryResult } from '@apollo/client/core';
import { CustomQuery } from '@vue-storefront/core';
import { CustomerOrdersQuery } from '../../types/GraphQL';
import { Context } from '../../types/context';
import { GetOrdersSearchParams } from '../../types/API';
/**
 * Returns customer orders. To override the default query, use the `customerOrders` query key.
 */
declare const _default: (context: Context, searchParams: GetOrdersSearchParams, customQuery?: CustomQuery) => Promise<ApolloQueryResult<CustomerOrdersQuery>>;
export default _default;
