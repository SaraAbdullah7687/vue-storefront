import { ApolloQueryResult } from '@apollo/client/core';
import { CustomQuery } from '@vue-storefront/core';
import { ProductReviewQuery } from '../../types/GraphQL';
import { Context } from '../../types/context';
import { GetProductSearchParams } from '../../types/API';
/**
 * Returns reviews of the provided product
 */
declare const _default: (context: Context, searchParams?: GetProductSearchParams, customQuery?: CustomQuery) => Promise<ApolloQueryResult<ProductReviewQuery>>;
export default _default;
