import { ApolloQueryResult } from '@apollo/client/core';
import { CustomQuery } from '@vue-storefront/core';
import { CustomerProductReviewQuery } from '../../types/GraphQL';
import { Context } from '../../types/context';
export declare type CustomerProductReviewParams = {
    pageSize: number;
    currentPage: number;
};
/**
 * Returns product reviews created by the current customer
 */
declare const _default: (context: Context, searchParams?: CustomerProductReviewParams, customQuery?: CustomQuery) => Promise<ApolloQueryResult<CustomerProductReviewQuery>>;
export default _default;
