import { ApolloQueryResult } from '@apollo/client/core';
import { CustomQuery } from '@vue-storefront/core';
import { ProductReviewRatingsMetadataQuery } from '../../types/GraphQL';
import { Context } from '../../types/context';
/**
 * Returns additional product reviews data
 */
declare const _default: (context: Context, customQuery?: CustomQuery) => Promise<ApolloQueryResult<ProductReviewRatingsMetadataQuery>>;
export default _default;
