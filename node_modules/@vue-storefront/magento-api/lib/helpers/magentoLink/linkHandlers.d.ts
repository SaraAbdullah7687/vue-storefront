import { ConfigState } from '../../types/setup';
export declare const handleRetry: () => (count: any, operation: any, error: any) => boolean;
export declare const linkFactory: ({ state }: {
    state: ConfigState;
}) => import("@apollo/client/core").ApolloLink;
